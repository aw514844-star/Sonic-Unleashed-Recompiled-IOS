name: Sonic Unleashed Final - BUILD 209
on: [workflow_dispatch, push]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Dependencies
        run: brew install cmake ninja pkg-config

      - name: Generate & Build (Metal/iOS)
        run: |
          mkdir -p build_ios
          
          # We still tell CMake where Freetype is, but we let the compiler 
          # handle the header search paths more broadly to avoid "file not found"
          cmake -S . -B build_ios \
            -G Xcode \
            -DCMAKE_SYSTEM_NAME=iOS \
            -DCMAKE_OSX_DEPLOYMENT_TARGET=15.0 \
            -DCMAKE_XCODE_ATTRIBUTE_CODE_SIGNING_ALLOWED=NO \
            -DCMAKE_XCODE_ATTRIBUTE_CODE_SIGNING_REQUIRED=NO \
            -DCMAKE_XCODE_ATTRIBUTE_AD_HOC_CODE_SIGNING_ALLOWED=YES \
            -DCMAKE_XCODE_ATTRIBUTE_ENABLE_BITCODE=NO

          # Explicitly target the iphoneos SDK during the build phase
          cmake --build build_ios --config Release --target UnleashedRecompiOSD -- -sdk iphoneos

      - name: Construct Verified IPA
        run: |
          # 1. SETUP DIRECTORIES
          rm -rf Payload Unleashed_Final.ipa
          mkdir -p Payload/Unleashed.app/private
          
          # 2. LOCATE THE BUILT BUNDLE
          # Xcode places iOS builds in a specific subdirectory
          BUILT_APP=$(find build_ios -name "UnleashedRecompiOSD.app" -type d -path "*/Release-iphoneos/*" | head -n 1)
          
          if [ -z "$BUILT_APP" ]; then 
            echo "ERROR: Could not find the .app bundle in build_ios/Release-iphoneos"
            exit 1
          fi
          
          # 3. ASSEMBLE IPA CONTENT
          cp -r "$BUILT_APP/" Payload/Unleashed.app/
          
          # 4. INJECT ASSETS
          if [ -d "UnleashedRecompLib/private" ]; then
            cp -v UnleashedRecompLib/private/* Payload/Unleashed.app/private/
          fi

          # 5. CREATE CONFIG
          cat <<EOF > Payload/Unleashed.app/config.toml
          [Video]
          GraphicsAPI = "Metal"
          ResolutionScale = 1.0
          FrameRateLimit = 60
          FullScreen = true
          EOF

          # 6. GENERATE INFO.PLIST
          cat <<EOF > Payload/Unleashed.app/Info.plist
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>CFBundleExecutable</key><string>UnleashedRecompiOSD</string>
              <key>CFBundleIdentifier</key><string>com.unleashed.recomp</string>
              <key>CFBundleName</key><string>Unleashed</string>
              <key>CFBundlePackageType</key><string>APPL</string>
              <key>CFBundleShortVersionString</key><string>1.0</string>
              <key>LSRequiresIPhoneOS</key><true/>
              <key>MinimumOSVersion</key><string>15.0</string>
              <key>UIFileSharingEnabled</key><true/>
              <key>LSSupportsOpeningDocumentsInPlace</key><true/>
              <key>UIRequiresFullScreen</key><true/>
              <key>UIDeviceFamily</key><array><integer>2</integer></array>
              <key>UISupportedInterfaceOrientations</key>
              <array>
                  <string>UIInterfaceOrientationLandscapeLeft</string>
                  <string>UIInterfaceOrientationLandscapeRight</string>
              </array>
          </dict>
          </plist>
          EOF

          # 7. FINAL BINARY CHECK
          if [ ! -f "Payload/Unleashed.app/UnleashedRecompiOSD" ]; then
            echo "CRITICAL ERROR: Binary is missing from the app bundle!"
            exit 1
          fi
          
          # 8. ZIP IT UP
          zip -ry Unleashed_Final.ipa Payload

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Unleashed-iPad-Success
          path: Unleashed_Final.ipa
