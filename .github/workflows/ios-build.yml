name: Build Sonic Unleashed iOS (Metal + Assets + Icon)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build_ios:
    runs-on: macos-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Dependencies
        run: |
          brew install cmake ninja pkg-config freetype curl fmt sdl2 sdl2_mixer sdl2_ttf

      - name: Verify Assets
        run: |
          if [ ! -f "UnleashedRecompLib/private/default.xex" ]; then
            echo "::error::Assets missing! Ensure default.xex is in UnleashedRecompLib/private/"
            exit 1
          fi

      - name: Configure and Build
        run: |
          # Targeting Metal and arm64 for iPad performance
          cmake . -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_SYSTEM_NAME=iOS \
            -DCMAKE_OSX_SYSROOT=iphoneos \
            -DCMAKE_OSX_ARCHITECTURES=arm64 \
            -DENABLE_METAL=ON \
            -DGRAPHICS_API=Metal \
            -DUNLEASHED_RECOMP_IOS_ENABLE_CODE_SIGNING=OFF \
            -DCMAKE_PREFIX_PATH="$(brew --prefix fmt);$(brew --prefix sdl2);$(brew --prefix sdl2_mixer);$(brew --prefix sdl2_ttf)" \
            -DCURL_LIBRARY=$(brew --prefix curl)/lib/libcurl.dylib \
            -DCURL_INCLUDE_DIR=$(brew --prefix curl)/include \
            -DFREETYPE_LIBRARY=$(brew --prefix freetype)/lib/libfreetype.dylib \
            -DFREETYPE_INCLUDE_DIRS=$(brew --prefix freetype)/include/freetype2
          
          cmake --build build --target UnleashedRecomp

      - name: Create Full IPA
        run: |
          mkdir -p Payload/Unleashed.app

          # Find the engine binary and move it to the app folder
          BINARY_PATH=$(find ./build -name "UnleashedRecomp" -type f -perm +111 | head -n 1)
          cp "$BINARY_PATH" Payload/Unleashed.app/UnleashedRecomp
          chmod +x Payload/Unleashed.app/UnleashedRecomp

          # Copy your private assets (default.xex, shader.ar) into the bundle
          mkdir -p Payload/Unleashed.app/private
          cp -R UnleashedRecompLib/private/. Payload/Unleashed.app/private/

          # Apply the Icon
          if [ -f "icon.png" ]; then
            cp icon.png Payload/Unleashed.app/icon.png
          fi

          # Plist with Metal, File Sharing, and Icon support
          cat <<EOF > Payload/Unleashed.app/Info.plist
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://apple.com">
          <plist version="1.0">
          <dict>
              <key>CFBundleExecutable</key>
              <string>UnleashedRecomp</string>
              <key>CFBundleIdentifier</key>
              <string>com.unleashed.recomp</string>
              <key>CFBundleName</key>
              <string>Unleashed</string>
              <key>CFBundlePackageType</key>
              <string>APPL</string>
              <key>CFBundleShortVersionString</key>
              <string>1.0</string>
              <key>CFBundleVersion</key>
              <string>1</string>
              <key>LSRequiresIPhoneOS</key>
              <true/>
              <key>MinimumOSVersion</key>
              <string>15.0</string>
              <key>UIFileSharingEnabled</key>
              <true/>
              <key>LSSupportsOpeningDocumentsInPlace</key>
              <true/>
              <key>CFBundleIconFiles</key>
              <array><string>icon.png</string></array>
              <key>UIDeviceFamily</key>
              <array><integer>1</integer><integer>2</integer></array>
          </dict>
          </plist>
          EOF

          zip -ry Sonic_Unleashed_Metal.ipa Payload

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: Unleashed-Metal-IPA
          path: Sonic_Unleashed_Metal.ipa
