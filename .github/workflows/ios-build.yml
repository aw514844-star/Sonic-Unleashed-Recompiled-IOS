name: Build Sonic Unleashed iOS (Emergency Final Fix)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build_ios:
    runs-on: macos-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Build Tools
        run: brew install cmake ninja pkg-config

      - name: Setup vcpkg (Full History Fix)
        run: |
          rm -rf vcpkg
          # REMOVED --depth 1 to fix the 'baseline.json' git error
          git clone https://github.com vcpkg
          ./vcpkg/bootstrap-vcpkg.sh
          # DELETE vcpkg.json if it exists to prevent Manifest Mode conflicts
          rm -f vcpkg.json
          ./vcpkg/vcpkg install sdl2 freetype fmt curl --triplet arm64-ios

      - name: Configure and Build (Metal Optimized)
        run: |
          # Explicitly point to Ninja to fix 'CMAKE_MAKE_PROGRAM not set'
          cmake . -B build -G Ninja \
            -DCMAKE_MAKE_PROGRAM=$(which ninja) \
            -DCMAKE_TOOLCHAIN_FILE=$(pwd)/vcpkg/scripts/buildsystems/vcpkg.cmake \
            -DVCPKG_TARGET_TRIPLET=arm64-ios \
            -DCMAKE_SYSTEM_NAME=iOS \
            -DCMAKE_OSX_SYSROOT=iphoneos \
            -DCMAKE_OSX_ARCHITECTURES=arm64 \
            -DCMAKE_BUILD_TYPE=Release \
            -DENABLE_METAL=ON \
            -DUNLEASHED_RECOMP_IOS_ENABLE_CODE_SIGNING=OFF \
            -DCMAKE_XCODE_ATTRIBUTE_CODE_SIGNING_REQUIRED=NO \
            -DCMAKE_CXX_FLAGS="-O3 -flto"
          
          cmake --build build --target UnleashedRecomp

      - name: Validate and Package IPA
        run: |
          mkdir -p Payload/Unleashed.app/private
          BINARY_PATH=$(find ./build -name "UnleashedRecomp" -type f -perm +111 | head -n 1)
          if [ -z "$BINARY_PATH" ]; then exit 1; fi
          cp "$BINARY_PATH" Payload/Unleashed.app/UnleashedRecomp
          chmod +x Payload/Unleashed.app/UnleashedRecomp
          if [ -d "UnleashedRecompLib/private" ]; then
            cp -R UnleashedRecompLib/private/. Payload/Unleashed.app/private/
          fi
          cat <<EOF > Payload/Unleashed.app/Info.plist
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://apple.com">
          <plist version="1.0"><dict>
              <key>CFBundleExecutable</key><string>UnleashedRecomp</string>
              <key>CFBundleIdentifier</key><string>com.unleashed.recomp</string>
              <key>CFBundleName</key><string>Unleashed</string>
              <key>CFBundlePackageType</key><string>APPL</string>
              <key>CFBundleShortVersionString</key><string>1.0</string>
              <key>LSRequiresIPhoneOS</key><true/>
              <key>MinimumOSVersion</key><string>15.0</string>
              <key>UIFileSharingEnabled</key><true/>
              <key>LSSupportsOpeningDocumentsInPlace</key><true/>
              <key>com.apple.developer.kernel.increased-memory-limit</key><true/>
          </dict></plist>
          EOF
          zip -0ry Sonic_Unleashed_iPad9.ipa Payload

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: Sonic-Unleashed-IPA
          path: Sonic_Unleashed_iPad9.ipa
