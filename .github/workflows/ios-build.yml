name: Build Sonic Unleashed iOS (iPad 9 Optimized)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build_ios:
    runs-on: macos-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Dependencies
        run: brew install cmake ninja pkg-config freetype curl fmt sdl2 sdl2_mixer sdl2_ttf

      - name: Configure and Build (3GB RAM Optimization)
        run: |
          # Added memory-related flags to help with iPad 9 constraints
          cmake . -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_SYSTEM_NAME=iOS \
            -DCMAKE_OSX_SYSROOT=iphoneos \
            -DCMAKE_OSX_ARCHITECTURES=arm64 \
            -DENABLE_METAL=ON \
            -DGRAPHICS_API=Metal \
            -DUNLEASHED_RECOMP_IOS_ENABLE_CODE_SIGNING=OFF \
            -DCMAKE_CXX_FLAGS="-O3 -flto" \
            -DCMAKE_PREFIX_PATH="$(brew --prefix fmt);$(brew --prefix sdl2);$(brew --prefix sdl2_mixer);$(brew --prefix sdl2_ttf)"
          
          cmake --build build --target UnleashedRecomp

      - name: Create Optimized IPA
        run: |
          mkdir -p Payload/Unleashed.app
          BINARY_PATH=$(find ./build -name "UnleashedRecomp" -type f -perm +111 | head -n 1)
          cp "$BINARY_PATH" Payload/Unleashed.app/UnleashedRecomp
          chmod +x Payload/Unleashed.app/UnleashedRecomp

          # Copy assets to internal private folder
          mkdir -p Payload/Unleashed.app/private
          cp -R UnleashedRecompLib/private/. Payload/Unleashed.app/private/

          # Entitlements for 3GB RAM iPads (Critical for iPad 9)
          cat <<EOF > Payload/Unleashed.app/archived-expanded-entitlements.xcent
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://apple.com">
          <plist version="1.0">
          <dict>
              <key>com.apple.developer.kernel.increased-memory-limit</key>
              <true/>
          </dict>
          </plist>
          EOF

          # Info.plist with File Sharing and Icon
          cat <<EOF > Payload/Unleashed.app/Info.plist
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://apple.com">
          <plist version="1.0">
          <dict>
              <key>CFBundleExecutable</key>
              <string>UnleashedRecomp</string>
              <key>CFBundleIdentifier</key>
              <string>com.unleashed.recomp</string>
              <key>CFBundleName</key>
              <string>Unleashed</string>
              <key>CFBundlePackageType</key>
              <string>APPL</string>
              <key>CFBundleShortVersionString</key>
              <string>1.0</string>
              <key>MinimumOSVersion</key>
              <string>15.0</string>
              <key>UIFileSharingEnabled</key>
              <true/>
              <key>LSSupportsOpeningDocumentsInPlace</key>
              <true/>
              <key>CFBundleIconFiles</key>
              <array><string>icon.png</string></array>
          </dict>
          </plist>
          EOF

          if [ -f "icon.png" ]; then cp icon.png Payload/Unleashed.app/icon.png; fi
          zip -ry Sonic_Unleashed_iPad9.ipa Payload

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: Unleashed-iPad9-IPA
          path: Sonic_Unleashed_iPad9.ipa
